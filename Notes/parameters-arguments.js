// Parametros son los nombres que usas en la declaracion de la funcion
// const arrowFunction = (PARAMETROS) => {}
// Argumentos son los valores reales que le pasas a la funcion al ejecutarla
// arrowFunction(ARGUMENTOS);