/** DATA TYPES
 * Primitive Data
 *  number 1
 *  string 'hola'
 *  boolean true|false
 *  undefined
 *  null (object)
 *  symbol
 * 
 * 
 * Compound Data ( Se crea con const (casi)siempre - Son mutables)
 *  object {}   (diccionario/hashtable) (Object.keys(objX) retorna un array con las keys del objeto)
 *  array []    (son objetos realmente)
 * 
 * 
 * CONDITIONALS
 *  if () {} | else
 *  switch () {} | case: | default:
 * 
 * LOOPS
 *  while (a === b) {}
 *  for (i = x; i < z; i++)
 * 
 *  Las function tambien son hoisteables si no se crean en una variable de scope de bloque
 *  Solo el + concatena si se usan datos de otros tipos 
 *  El === compara si ambos datos son exactamente iguales, del mismo tipo, etc.,
 *      mientras que el == no, puede convertir un dato a otro tipo de dato para realizar su condicion.
 *  En function(...rest) puedes usar ... (Spread Operator) al declarar un parametro para referirte al resto de variables
 * 
 * 
 * 
 * 
 * */

