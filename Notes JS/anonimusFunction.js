// Las funciones anonimas se usan cuando son funciones que solo se van a usar una vez (si las fueras
// a usar varias veces mejor la declaras y luego solo la llamas cuando la necesites), y de
// preferencia que sean cortas (unas cuantas lineas de codigo).